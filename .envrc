use flake

export UV_LOCKFILE=uv.dev.lock

if [ "$TERM_PROGRAM" != "vscode" ]; then
  if [ -d .venv ] && [ -z "$VIRTUAL_ENV" ]; then
    echo "Activating Python venv (.venv)"
    source .venv/bin/activate
  fi
fi
